import { WorldObject } from "../world-model";

export function buildObjects(
  objectBible: unknown
): WorldObject[] {
  if (!objectBible) {
    return [];
  }

  // TODO:
  // objectBible から WorldObject[] を抽出する。
  return [];
}